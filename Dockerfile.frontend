# 开发版 Dockerfile (Development)
FROM node:18-alpine

WORKDIR /app

# 先复制依赖文件，利用 Docker 缓存
COPY web/package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY web/ .

# 暴露 Vite 端口
EXPOSE 5173

# 启动开发服务器 (关键是这一步)
CMD ["npm", "run", "dev", "--", "--host"]